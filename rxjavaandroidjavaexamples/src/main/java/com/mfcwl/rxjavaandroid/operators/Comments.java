package com.mfcwl.rxjavaandroid.operators;

public class Comments {
    /*Purpose of an Operator ?*/
    /*The Purpose of an Operator is to take an given data set and transform it into an Observable data set.
     * Some Operators can also manipulate the data objects and transform them further.*/

    /*Create Observables Operators*/
    /*create(), fromIterable(), fromArray(), just(), range(), repeat()*/

    /*Filtering & Sorting Operators*/
    /*filter(), distinct(), take(), takeLast(), skip()*/

    /*Transform emitted data into other types Operators*/
    /*map(), flatMap(), switchMap(), buffer(), contactMap()*/
}
